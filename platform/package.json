{
  "name": "blog-os-platform",
  "version": "1.0.0",
  "description": "Blog OS — plataforma de conteúdo estática, zero dependências",
  "private": true,
  "scripts": {
    "build": "node src/build.js",
    "build:force": "node src/build.js --force",
    "check": "node src/check.js",
    "serve": "node src/serve.js"
  }
}
